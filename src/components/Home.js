import React, { Component } from 'react';
import Layout from './../utils/Layout/layout';

class Home extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  render() {
    return (
      <div>
        <Layout />
      </div>
    );
  }
}

export default Home;
